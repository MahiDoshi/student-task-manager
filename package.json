{
  "name": "student-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "A simple student task manager",
  "scripts": {
    "test": "node --test",
    "lint": "node --check script.js",
    "build": "node --check script.js"
  }
}
